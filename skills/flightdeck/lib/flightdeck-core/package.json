{
  "name": "flightdeck-core",
  "private": true,
  "type": "module",
  "description": "TypeScript implementation of the flightdeck skill scripts (daemon, state, registry, classifier, pane I/O, protocol adapters). Trampolined from skills/flightdeck/scripts/*.",
  "scripts": {
    "test": "bun test",
    "test:parity": "bun test tests/parity",
    "test:unit": "bun test tests/unit",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^5.6.0"
  }
}
